import { ConfigClient } from "./_components/config-client";

export const metadata = {
  title: "Config · Cazador Globalizame",
};

export default function ConfigPage() {
  return <ConfigClient />;
}
