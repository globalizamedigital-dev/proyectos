# LeadHunter Pro — Dependencias
#
# NÚCLEO: Solo stdlib Python 3.11+ — no se requiere instalar nada para la
# funcionalidad principal (scraping, SMTP verify, GUI Tkinter, SQLite, CSV).
#
# OPCIONALES: Mejoran funcionalidad si están disponibles (degradación elegante si no).

# DNS para verificación SMTP más robusta (fallback a socket si no está)
dnspython>=2.4.0

# Excel export (si se quiere exportar a .xlsx además de CSV)
openpyxl>=3.1.0

# Scraping avanzado con JavaScript (para webs SPA/React que no cargan con urllib)
# playwright>=1.40.0
# Para instalar: pip install playwright && playwright install chromium

# HTTP con mejor manejo de cookies/sesiones (opcional, urllib funciona bien)
# requests>=2.31.0

# Mejor parsing HTML (opcional, BeautifulSoup mejora extracción de contactos)
# beautifulsoup4>=4.12.0
# lxml>=4.9.0

# Testing
pytest>=7.4.0
